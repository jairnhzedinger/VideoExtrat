# Repository Guidelines

- Place source code in the `src/` directory.
- Use Python 3.10 compatible code.
- Keep code formatting PEP8 compliant.
- Add new tests under the `tests/` directory when implementing features.
- After making changes, run `python -m compileall .` to verify Python files.
